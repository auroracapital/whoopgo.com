# v1.0 Addendum — Out-of-Band Work (2026-04-24)

After v1.0 shipped 2026-04-16 and was archived 2026-04-22, a large block of post-launch work was executed out-of-band on 2026-04-24 without a formal milestone. This addendum codifies that work so it isn't lost to history. It is effectively "v1.0.1" — trust, identity, and consolidation hardening — and it sets the stage for the formal v1.1 milestone.

## Summary

| # | Area | Description | Evidence |
|---|------|-------------|----------|
| 1 | Domain | Registered `whoopgo.app`, set as canonical. `whoopgo.com` retained as 301 redirect. | PR #12, `.planning/DOMAIN-whoopgo-app.md` |
| 2 | DNS / SSL | Cloudflare zone `2e0ebaa425654d6a37640fdafaa4534e` configured: A/CNAME records, SSL issued by Let's Encrypt via Render. | Cloudflare zone + Render custom domains `cdm-d7ligt3eo5us73dlbkjg` (apex), `cdm-d7ligt3eo5us73dlbkkg` (www). |
| 3 | Email Routing | Cloudflare Email Routing: `support@whoopgo.app` → `info@auroracapital.nl`. | Cloudflare Email config. |
| 4 | Auth — Clerk Webhook | `/api/webhooks/clerk` endpoint shipped with Svix signature verification. | PR #11. DB persistence deferred to v1.1 Phase 6. |
| 5 | Auth — Google SSO | Wired in Clerk production. | Clerk dashboard. |
| 6 | Auth — Apple SSO | Wired in Clerk production (requires paid Apple Developer). | Clerk dashboard. |
| 7 | Auth — Facebook SSO | Wired in Clerk; underlying FB app is in **Development mode** (end users blocked). Live-mode flip deferred to v1.1 Phase 11. | Clerk dashboard + Meta Business Manager. |
| 8 | AI Provider | `/api/chat` swapped from Anthropic Claude → Gemini 3.1 Flash-Lite. Reduces cost, improves latency. | PR #13. |
| 9 | Legal — Terms | `/terms` page shipped, then rewritten under Lifecycle Innovations Limited (HK), BRN 76545088. DRAFT banner present — real legal review deferred to v1.1 Phase 8. | PRs #14, #15. |
| 10 | Legal — Privacy | `/privacy` page shipped under same HK entity. DRAFT banner present. | PRs #14, #15. |
| 11 | Contact | Contact info standardized: `support@whoopgo.app`, HK registered office address. | PR #16. |
| 12 | eSIM Provider | Consolidated to single upstream — **eSIMVault via `esimmcp` only**. Airalo / eSIM.travel paths removed. Reduces surface area, single SLA to manage. | Codebase change (provider abstraction retained, only one concrete provider). |
| 13 | Render | Custom domain attached, env hygiene pass (BASE_URL=https://whoopgo.app). | Render service `srv-d7akcrfkijhs73dp5c3g`. |

## Known issues carried forward

- **Clerk webhook is receive-only**: signature verified + logged; does not write to DB. → v1.1 Phase 6.
- **SSO is untested end-to-end**: no smoke suite hitting real providers. → v1.1 Phase 7.
- **Terms + Privacy flagged DRAFT**: not yet counsel-reviewed. → v1.1 Phase 8.
- **Facebook login blocked for non-admins**: app still in Development mode. → v1.1 Phase 11.
- **ANTHROPIC_API_KEY placeholder**: inadvertently overwritten on Render during env PUT (see `DOMAIN-whoopgo-app.md`). Obsolete now that chat moved to Gemini — key can be removed entirely.

## Why this wasn't a formal milestone

All of this work was reactive/enabling — DNS, legal scaffolding, provider swaps — needed to unblock the next real feature milestone (v1.1). Codifying it here preserves the audit trail without retroactively rewriting v1.0's scope.
